*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
local.properties

# Release signing — never commit the keystore or its passwords.
# keystore.properties.example is committed as a template; the real file isn't.
keystore.properties
*.jks
*.keystore

# Kotlin compiler session data
.kotlin/

# Claude Code local state (worktrees, local settings)
/.claude/

# Crash dumps and scratch build logs
hs_err_pid*.log
replay_pid*.log
*.hprof
/build-*.log
/build-log.txt
/build_*.txt
/build_*.log

# session build/verify logs (local only)
compile-verify.log
*.verify.log

# Build outputs handed to the owner for device testing. They are rebuilt from
# any commit and weigh ~25 MB each; two were already sitting untracked in the
# root, one command away from being committed by accident.
*.apk
