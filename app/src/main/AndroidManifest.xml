<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Bluetooth permissions for thermal ESC/POS printing (added in a later milestone).
         Declared up front so the printer module can request them at runtime. -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />

    <!-- Internet for Supabase sync (added in a later milestone). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Camera for barcode scanning (CameraX + zxing). Optional hardware. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Call log: "recent callers" quick-add on the Customers screen (Phase 4).
         Requested at runtime; the feature degrades to hidden if denied. The
         contact/number picker used to fill a customer's details needs NO
         permission (the system picker grants a one-shot read on the chosen row). -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" />

    <!-- Mobile-money SMS reconciliation (Phase 5, §6). RECEIVE_SMS delivers the
         confirmation message to SmsReceiver; READ_SMS lets a future backfill re-scan
         the inbox. Sideload-only distribution makes these acceptable (§5). Requested
         at runtime; if denied, reconciliation simply stays empty. -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <!-- Post the "payment received — verify" notification (Android 13+ needs a
         runtime grant; declaration alone is not enough). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Keep delivering payment SMS + admin alerts in the background: ask the owner to
         exempt the app from Doze / battery optimization (§6/§8). Standard for a POS;
         prompted once. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <application
        android:name=".PosApp"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpotPos">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:label="@string/app_name"
            android:theme="@style/Theme.SpotPos"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Mobile-money SMS listener (Phase 5, §6). Guarded by BROADCAST_SMS so only
             the system can deliver the broadcast; exported because SMS_RECEIVED is a
             system broadcast. -->
        <receiver
            android:name=".sms.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- Share/re-open generated PDFs (Phase 6, §5) via a content:// URI. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
